import { colors } from './colors';
import { spacing, fontSize, borderRadius, shadows, screen, puzzleGrid } from './dimensions';

export const theme = {
  colors,
  spacing,
  fontSize,
  borderRadius,
  shadows,
  screen,
  puzzleGrid,
};

export { colors } from './colors';
export { spacing, fontSize, borderRadius, shadows, screen, puzzleGrid } from './dimensions';
